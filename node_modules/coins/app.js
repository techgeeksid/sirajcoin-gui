let lotion = require('lotion')
let coins = require('./')
let htlc = require('../htlc')

let app = lotion({
  devMode: true //,
  // logTendermint: true
})

app.use(coins({
  handlers: { htlc },
  initialBalances: {
    xdyP1cAUX2B1CjNXmVPx62sQF6s: 1e6
  }
}))

app.listen(8888)
